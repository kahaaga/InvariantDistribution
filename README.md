# InvariantDistribution.jl

Julia library for estimating the transfer operator for a state space reconstruction (from time series). Functions to compute invariant probability distributions are also included.
